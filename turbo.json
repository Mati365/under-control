{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "lint": {
      "dependsOn": ["build"],
      "outputs": []
    },
    "test": {
      "outputs": [],
      "inputs": ["src/**/*.test.tsx", "src/**/*.test.ts"],
      "cache": false
    },
    "deploy": {
      "dependsOn": ["build", "lint", "test"],
      "outputs": []
    }
  }
}
